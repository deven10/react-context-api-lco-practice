import React from "react";

export default React.createContext();  // createContext helps us to create a context that 
                                       // will pass the data from one component to another
                                       // instead of using Props everywhere context is a better option.